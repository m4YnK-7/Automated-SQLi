# Lab Authorization & Safety

This environment is permitted for use only by the repository owner on isolated networks or VMs under explicit authorization.

- Do not run scans against external or production systems.
- Ensure lab network has no internet egress unless explicitly allowed.
- Keep raw exploit logs private; do not commit sensitive payloads.

Sign-off: Repository owner / lab supervisor
EOF
