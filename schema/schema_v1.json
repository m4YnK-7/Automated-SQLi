{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "AutomatedSQLi Log Schema v1",
  "description": "Unified JSON-lines schema for proxy, orchestrator, db_logger and analysis components.",
  "type": "object",
  "properties": {
    "timestamp": {
      "type": "string",
      "format": "date-time",
      "description": "ISO8601 UTC timestamp when the event was observed (e.g. 2025-11-09T09:32:15Z)."
    },
    "trace_id": {
      "type": "string",
      "pattern": "^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$",
      "description": "Canonical trace id (UUIDv4 lowercase). Header name: X-Trace-ID."
    },
    "round": {
      "type": "integer",
      "minimum": 0,
      "description": "Experiment / scanning round (1..n)."
    },
    "tool": {
      "type": "string",
      "enum": ["proxy", "scanner", "orchestrator", "db_logger", "analyzer"],
      "description": "Which component emitted the record."
    },
    "payload_id": {
      "type": "string",
      "description": "Component-local payload identifier (opaque)."
    },

    "src_ip": {
      "type": "string",
      "description": "Source IP address (client or internal caller). Store as string — sanitize in upstream if necessary."
    },
    "method": {
      "type": "string",
      "enum": ["GET", "POST", "PUT", "DELETE", "PATCH", "OPTIONS", "HEAD"],
      "description": "HTTP method."
    },
    "url": {
      "type": "string",
      "description": "Request URL (path + query)."
    },
    "headers": {
      "type": "object",
      "description": "Representative headers. Components SHOULD NOT log full sensitive headers (see schema.md)."
    },
    "params": {
      "type": "object",
      "description": "Query/form parameters after sanitization/hashing as defined in schema.md."
    },
    "body_snippet": {
      "type": ["string", "null"],
      "maxLength": 512,
      "description": "Short, redacted snippet of request or payload for debugging (PII redacted)."
    },

    "response_code": {
      "type": ["integer", "null"],
      "description": "HTTP response status code observed."
    },
    "response_time_ms": {
      "type": ["integer", "null"],
      "minimum": 0,
      "description": "Response latency in milliseconds."
    },

    "db_query": {
      "type": ["string", "null"],
      "description": "Database query text (with sensitive tokens hashed). Prefer hashed placeholders, see schema.md."
    },
    "db_status": {
      "type": ["string", "null"],
      "enum": ["ok", "error", "warning", null]
    },

    "label": {
      "type": "string",
      "enum": ["benign", "suspicious", "exploit"],
      "description": "Classification label assigned by analyzer or operator."
    },
    "confidence": {
      "type": ["number", "null"],
      "minimum": 0,
      "maximum": 1,
      "description": "Optional classifier confidence (0..1)."
    },

    "meta": {
      "type": "object",
      "properties": {
        "version": { "type": "string" },
        "hash_salt_id": { "type": "string", "description": "Identifier for the salt key in secrets manager (do not store actual salt here)." },
        "notes": { "type": "string" }
      },
      "required": ["version"]
    }
  },

  "required": ["timestamp", "trace_id", "round", "tool", "payload_id", "src_ip", "method", "url", "label", "meta"],
  "additionalProperties": false
}
